/** Tailwind config for the compiled stylesheet (public/tailwind.css).
 * Rebuild after adding new utility classes:
 *   npm run build:css
 */
module.exports = {
  content: ['./public/**/*.html', './public/**/*.js'],
  theme: {
    extend: {}
  },
  plugins: []
};
